name: GiantSwarm Test Catalog Cleanup

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:
  push:

jobs:
  cleanup:
    uses: giantswarm/app-catalog-cleanup-tool/.github/workflows/cleanup.yaml@4fa9c643989952d3a6b9e65444cbb9f2d7fa4fda
    with:
      delete-before: "1 month"
      dry-run: true
