name: GiantSwarm Test Catalog Cleanup

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:
  push:

jobs:
  cleanup:
    uses: giantswarm/app-catalog-cleanup-tool/.github/workflows/cleanup.yaml@25fb1294f6e7420212cbfea2f7d50f4b38c2e28a
    with:
      delete-before: "1 month"
      dry-run: true
