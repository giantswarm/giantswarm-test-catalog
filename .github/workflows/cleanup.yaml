name: GiantSwarm Test Catalog Cleanup

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:
  push:

jobs:
  cleanup:
    uses: giantswarm/app-catalog-cleanup-tool/.github/workflows/cleanup.yaml@042408bf4fb3803275bf335fa46cbb54dd2d6e5b
    with:
      delete-before: "1 month"
      dry-run: true
