name: GiantSwarm Test Catalog Cleanup

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:
  push:

jobs:
  cleanup:
    uses: giantswarm/app-catalog-cleanup-tool/.github/workflows/cleanup.yaml@c675d369fb177598d4ec5160010ae6588276a5b8
    with:
      delete-before: "1 month"
      dry-run: false
