name: GiantSwarm Test Catalog Cleanup

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:
  push:

jobs:
  cleanup:
    uses: giantswarm/app-catalog-cleanup-tool/.github/workflows/cleanup.yaml@aae62846ce382cba81c0cf4d26368886447b75e7
    with:
      delete-before: "1 month"
      dry-run: true
