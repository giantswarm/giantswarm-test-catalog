name: GiantSwarm Test Catalog Cleanup

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:
  push:

jobs:
  cleanup:
    uses: giantswarm/app-catalog-cleanup-tool/.github/workflows/cleanup.yaml@cfcbb16022d5599a659b3ac31818d28b556952a0
    with:
      delete-before: "1 month"
      dry-run: true
