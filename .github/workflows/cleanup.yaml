name: GiantSwarm Test Catalog Cleanup

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:
  push:

jobs:
  cleanup:
    uses: giantswarm/app-catalog-cleanup-tool/.github/workflows/cleanup.yaml@a8148526b7965b2ac598d8871baf5e8e4dab18e5
    with:
      delete-before: "1 month"
      dry-run: true
