name: GiantSwarm Test Catalog Cleanup

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:
  push:

jobs:
  cleanup:
    uses: giantswarm/app-catalog-cleanup-tool/.github/workflows/cleanup.yaml@11ed5ef85b5e942765d1280c5f4a7113a1225451
    with:
      delete-before: "1 month"
      dry-run: false
