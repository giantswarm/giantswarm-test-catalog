name: GiantSwarm Test Catalog Cleanup

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:
  push:

jobs:
  cleanup:
    uses: giantswarm/app-catalog-cleanup-tool/.github/workflows/cleanup.yaml@bd3d6a264b4fb20c565b5cdc4b2319a966dc3692
    with:
      delete-before: "1 month"
      dry-run: true
