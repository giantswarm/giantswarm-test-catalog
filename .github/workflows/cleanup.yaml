name: GiantSwarm Test Catalog Cleanup

on: push

jobs:
  cleanup:
    uses: giantswarm/app-catalog-cleanup-tool/.github/workflows/cleanup.yaml@version-upgrades
    with:
      delete-before: "1 month"
      dry-run: false
