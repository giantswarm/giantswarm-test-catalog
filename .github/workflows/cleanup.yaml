name: GiantSwarm Test Catalog Cleanup

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:
  push:

jobs:
  cleanup:
    uses: giantswarm/app-catalog-cleanup-tool/.github/workflows/cleanup.yaml@10fcd66bedf82464732a4f806b2ccae9a4a039c2
    with:
      delete-before: "1 month"
      dry-run: true
